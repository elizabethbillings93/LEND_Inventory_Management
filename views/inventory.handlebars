  <div class="employee-box">Employee #</div>
  {{#each categories as |category|}}
    <div>
        <p>
            <button>{{category.category_name}}</button>
        </p>
    </div>
  {{/each}}
  </div>
